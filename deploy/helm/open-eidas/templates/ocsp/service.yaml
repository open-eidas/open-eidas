{{- $name := printf "%s-ocsp" (include "open-eidas.fullname" .) -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ $name }}
  labels:
    {{- include "open-eidas.componentLabels" (dict "context" . "component" "ocsp") | nindent 4 }}
spec:
  selector:
    {{- include "open-eidas.componentSelectorLabels" (dict "context" . "component" "ocsp") | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.ocsp.service.port }}
      targetPort: http
