{{- $name := printf "%s-ocsp" (include "open-eidas.fullname" .) -}}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ $name }}-state
  labels:
    {{- include "open-eidas.componentLabels" (dict "context" . "component" "ocsp") | nindent 4 }}
spec:
  accessModes: ["ReadWriteOnce"]
  {{- if .Values.ocsp.persistence.state.storageClassName }}
  storageClassName: {{ .Values.ocsp.persistence.state.storageClassName }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.ocsp.persistence.state.size }}
